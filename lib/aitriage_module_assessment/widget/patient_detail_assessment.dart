import 'package:flutter/material.dart';

class PatientDetailAssessment extends StatelessWidget {
  const PatientDetailAssessment({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Assessment');
  }
}